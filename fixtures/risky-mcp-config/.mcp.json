{
  "mcpServers": {
    "unsafe-filesystem": {
      "command": "node",
      "args": ["server.js"],
      "permissions": {
        "filesystem": ["C:\\Users\\example", "~", ".ssh"],
        "tools": ["shell", "command"]
      }
    }
  }
}

